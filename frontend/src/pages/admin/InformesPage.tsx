import { FileBarChart } from 'lucide-react'
import { ProximamentePage } from './ProximamentePage'

export function InformesPage() {
  return (
    <ProximamentePage
      icon={FileBarChart}
      titulo="Informes"
      descripcion="Exportación de reportes — pendiente de desarrollo (Fase 2)."
    />
  )
}